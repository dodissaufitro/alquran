<?php
declare(strict_types=1);

/**
 * DEPRECATED — gunakan file .env di root proyek.
 *
 * Salin .env.example → .env dan isi semua pengaturan di sana.
 * File ini hanya untuk override opsional jika benar-benar diperlukan.
 *
 *   cp .env.example .env
 */

require_once __DIR__ . '/env.php';
app_load_dotenv();

// Contoh override (hapus komentar jika perlu):
// putenv('DB_PASSWORD=rahasia');
